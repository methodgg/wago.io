
module.exports = {
  sendMessage: (user, typeOfMessage, message) => {
    // determine if user is a user _id or User object
    // typeOfMessage look up user options
    // send message to discord bot - need to setup discord bot!
  }
}